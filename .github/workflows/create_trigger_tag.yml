name: '【auto】Trigger git tag'

on:
  push:
    tags:
      - 'v*'
      
jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Build and push excite-emininal-api Amazon ECR
        run: |
          echo "実行！！"
          pwd
